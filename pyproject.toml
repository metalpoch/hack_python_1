[project]
name = "hacks-1-python"
version = "0.1.0"
description = "Colección de hacks (problemas) de Python nivel 1 para practicar."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = "-v"
